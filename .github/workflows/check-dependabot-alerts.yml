name: Check Dependabot alerts and notify Slack

on:
  schedule:
    - cron: "0 5-14 * * 1-5"
  workflow_dispatch:

jobs:
  check_alerts:
    runs-on: ubuntu-26.04
    steps:
      - name: Check Dependabot alerts
        uses: tmfg/digitraffic-actions@76325c29bacab5e084d9434b8c37d8d8ced26e2d # dependabot-slack/v1
        with:
          TOKEN: ${{ secrets.DEPENDABOT_ALERTS_TOKEN }}
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_ALARMS_WEBHOOK_URL }}
          CRITICAL: "true"
          HIGH: "false"
          MEDIUM: "false"
          LOW: "false"
