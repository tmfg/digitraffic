#
#   Refer for explanation to following link:
#   https://lefthook.dev/configuration/
#

pre-commit:
  parallel: false
  commands:
    deno-fmt:
      #glob: "*.{ts,yml,yaml,md,json,html,css,scss" }"
      run: pnpm deno fmt {staged_files} && git add {staged_files}
